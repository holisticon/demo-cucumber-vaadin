# Pfad zum Browser Binary welches f�r headless tests verwendet werden soll
#
# - Eintrag wird nur verwendet wenn display gesetzt ist
# - momentan wird von der Implementierung nur firefox unterst�tzt  
#pathToBrowser=/opt/local/bin/firefox-x11
pathToBrowser=/usr/bin/firefox

# Wert der Systemvariable unter welcher das virtuelle Display f�r headless tests konfiguriert ist
#
# - f�r MacOS kann zum starten eines Xvfb (virtuelles Display) das entsprechende script im scripts folder verwendet werden
# - als Vorraussetzung wird XQuartz (X11 unter Mac) ben�tigt
# - falls nicht gesetzt wird nicht headless getestet (Browserfenster �ffnet sich w�hrend der Tests)
display=

# Url unter der die zu testende Anwendung erreichbar ist
applicationUrl=http://localhost:8080/demo-cucumber-vaadin-app/

# self explaining...
delayForRenderingInSeconds=3

# JDBC Url for database housekeeping
databaseUrl=

